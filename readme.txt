# Initialize your virtualenv using python -m venv [venv_name]
# Go in your virtualenv using cd venv_name
# Activate it using scripts\activate or source bin\activate
# Create a "Files" folder
# Run "yarn" or "npm"
# Run python(or python3) -m pip install -r requirements.txt to install requirements 
# Run "Python app.py" to start development server

